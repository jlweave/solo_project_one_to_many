<% @l_books.each do |book| %>
  <%= book.id %>
  <%= book.name %>
  <%= book.IBN %>
  <%= book.available %>
  <%= book.library_id %>
<% end %>

  <%= link_to "Create Book", "/libraries/#{@library.id}/books/new" %>
